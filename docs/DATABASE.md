# Database

No database is present in the MVP. Planned entities include astrologers, clients, birth profiles, locations, charts, calculation configurations, notes, and reports. Any future schema must be introduced through migrations and keep calculation inputs/configuration alongside reproducible outputs.
