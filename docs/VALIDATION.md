# Validation

No external reference dataset has been supplied yet. Current validation is internal consistency only and must not be treated as astronomical certification.

Reference comparisons should record input, convention, expected value, actual value, difference, tolerance, ephemeris version, and pass/fail status. Expected values must never be changed solely to make a test pass.
